<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml">
<body>
{{>menu}}
<style>
        td, th {
  border: 1px solid white;
  text-align: left;
  padding: 8px;
}

tr:nth-child(even) {
  background-color: white;
}
</style>
<div>
    <div class="row">
        <div class="col">
            <div style="font-size: 60px; margin-left: 1.5%;"><h2>{{candidate}}</h2></div>
        </div>
    </div>
</div>

<hr>

<div class="container">
    <div class="infobox">
        {{#message}}
            {{message}}
        {{/message}}

        <table>
            {{#election}}
                <tr>
                    <td> {{id}} </td>
                    <td> {{openDate}} </td>
                    <td> {{closeDate}} </td>
                    <td> <a href="/election/{{id}}"> Деталі </a> </td>
                </tr>
            {{/election}}
        </table>

    </div>
    <div>
        <form action="/candidate" method="get">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}">
            <input type="submit" value="Повернутися до кандидатів"/>
        </form>
    </div>
</div>

</body>
</html>